<UserControl x:Class="ModernApp.MVVM.View.SystemDiagnosisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModernApp.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Task Manager" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" />

        <!-- Main Content -->
        <TabControl Grid.Row="1">
            <!-- Processes Tab -->
            <TabItem Header="Processes">
                <Grid>
                    <DataGrid AutoGenerateColumns="False" CanUserAddRows="False" Margin="5" Name="ProcessesGrid">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Process Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="CPU (%)" Binding="{Binding CPU}" Width="100"/>
                            <DataGridTextColumn Header="Memory (MB)" Binding="{Binding Memory}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Performance Tab -->
            <TabItem Header="Performance">
                <StackPanel Margin="10">
                    <TextBlock Text="CPU Usage: 45%" FontSize="14" Margin="5"/>
                    <ProgressBar Value="45" Maximum="100" Height="20" Margin="5"/>
                    <TextBlock Text="Memory Usage: 4 GB / 8 GB" FontSize="14" Margin="5"/>
                    <ProgressBar Value="50" Maximum="100" Height="20" Margin="5"/>
                    <TextBlock Text="Disk Usage: 120 MB/s" FontSize="14" Margin="5"/>
                </StackPanel>
            </TabItem>

            <!-- Services Tab -->
            <TabItem Header="Services">
                <Grid>
                    <DataGrid AutoGenerateColumns="False" CanUserAddRows="False" Margin="5" Name="ServicesGrid">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Service Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
